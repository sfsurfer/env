# BASH ALIASES
alias gb='git branch'
alias co='git checkout'
alias cb='git branch | grep -E "^\*" | awk '\''{print $2}'\'''
alias gd='git diff'
alias gs='git status'
alias gc='git commit'
alias ff='git pull --ff'
alias dux='du * -cs | sort -nr | head'

